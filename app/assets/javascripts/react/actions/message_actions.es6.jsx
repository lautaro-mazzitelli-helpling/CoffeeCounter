"use strict";

class MessageActions {
  addMessage(message){
    return message;
  }
  removeLastMessage(){
    return message;
  }
}
window.app.actions.MessageActions = alt.createActions(MessageActions);