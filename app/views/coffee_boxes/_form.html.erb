<%= form_for(@coffee_box) do |f| %>
  <% if @coffee_box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coffee_box.errors.count, "error") %> prohibited this coffee_box from being saved:</h2>

      <ul>
      <% @coffee_box.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
